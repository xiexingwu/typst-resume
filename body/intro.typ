= Personal Statement
This block of content is intended to give a very brief introduction of the project.
Read more about the project in `README.md`.
By default, this block is placed in the `preamble` section of the resume's layout.
Other sections include the `core`, `side`, and `postamble`.
The profile picture can be replaced at `data/profile.jpg`.
The links above (e.g. website, social media handles, email) are configured in `main.typ`.
